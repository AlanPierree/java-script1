console.log("Atribuição de Variáveis")

const idade = 15;
// let (permitir, no inglês) declara uma variável que pode ser alterada
const primeiroNome = "Alan";
const sobrenome = "Pierre";

let contador = 0;
contador = contador + 1;

console.log(primeiroNome, "tem", idade, "anos.");
// Variáveis podem ser atribuídas dentro de strings usando "${}"
// Crase (`) também pode ser usado para declarar string
console.log(`Meu nome é ${primeiroNome} ${sobrenome}`);

nomeCompleto = (primeiroNome + sobrenome);

console.log(nomeCompleto);